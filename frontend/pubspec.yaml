name: income_expense_supabase
description: Offline-first income/expense tracker with Flutter + Firebase + Drift
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # State & navigation
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.0

  # Formatting
  intl: ^0.19.0

  # Local database (Drift + SQLite)
  drift: ^2.20.2
  drift_sqflite: ^2.0.1
  sqlite3_flutter_libs: ^0.5.24
  path_provider: ^2.1.3
  uuid: ^4.5.1

  # Firebase (Auth + Core)
  firebase_core: ^2.32.0
  firebase_auth: ^4.20.0

  # UI
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^4.0.0
  build_runner: ^2.4.11
  drift_dev: ^2.20.2

flutter:
  uses-material-design: true
